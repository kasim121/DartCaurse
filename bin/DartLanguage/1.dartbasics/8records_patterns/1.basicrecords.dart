/*
In Dart, Records and Patterns are powerful features that
 help manage and manipulate data structures in a concise and expressive way.
 They enhance both readability and flexibility in handling structured data.

1. Records in Dart
Records are lightweight data structures introduced in Dart 3.0 that
allow you to group related values without creating a custom class.
Unlike classes, records don’t require explicit field declarations,
making them simpler for small, temporary data structures.
*/

//ex. 1
/*
void main() {
  var res = printName();
  print(res);
}

(int, String) printName() {
  return (11, 'Kasim');
}


*/

void main() {
  // final {'userId': userId, 'id': id,'body':body,'title':title} = jsoned;
//   print(userId);
//   print(id);
//   print(body);
//   print(title);

  //print(jsoned['body']);//normal way to call
}
