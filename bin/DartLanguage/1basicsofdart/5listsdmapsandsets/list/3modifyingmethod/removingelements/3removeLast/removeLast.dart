void main() {
  // Using removeLast() to remove the last element
  List<int> numbers = [1, 2, 3, 4, 5];
  numbers.removeLast(); // Remove the last element (5)
  print(numbers); // Output: [1, 2, 3, 4]
}