/*
Method overloading typically refers to defining multiple methods in the same class 
with the same name but different parameter lists.
Dart does not support traditional method overloading in this sense.
In Dart, you cannot have multiple methods with the same name but different parameter types or
counts within the same class.

However, Dart supports a form of optional parameters and named parameters, which can achieve a similar effect:
*/