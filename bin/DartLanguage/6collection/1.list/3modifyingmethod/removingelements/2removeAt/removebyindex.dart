void main(){
    // Using removeAt() to remove an element by index
  List<String> fruits = ['apple', 'banana', 'orange'];
  fruits.removeAt(1); // Remove the element at index 1
  print(fruits); // Output: [apple, orange]
}